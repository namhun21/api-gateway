spring:
  application:
    name: eureka-server

server:
  port: 8761


eureka:
  client:
    #eureka 서버에 클라이언트로 등록여부
    register-with-eureka: false
    #eureka 서버로부터 받은 서비스 리스트에 대한 캐싱여부
    fetch-registry: false